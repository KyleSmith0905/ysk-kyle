name: Hourly cron job
on:
  schedule:
    - cron: '*/60 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Hourly cron job
        run: |
          curl --request POST \
          --url 'https://yskkyle.com/api/update-profile' \
          --header 'Authorization: Bearer ${{ secrets.ACTION_KEY }}'